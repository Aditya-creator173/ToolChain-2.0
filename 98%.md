# 98% Execution Plan (Actionable Now)

Goal: Reach a stable, near-final state quickly using only work we can execute now from available code and instructions.

## Priority Steps

- [x] **Step 1: Capture real failure traces (server + tests)**
   - Difficulty: 3/10
   - Est. Time: 10 min
   - Why now: We need exact, current errors to avoid fixing stale assumptions.
   - Output expected: Full traceback for `uvicorn` startup and full pytest failure list.

- [x] **Step 2: Verify package/layout truth before edits**
   - Difficulty: 2/10
   - Est. Time: 5 min
   - Why now: Import-path fixes depend on whether `tool_chain_env/server/` is the live source of truth.
   - Output expected: Confirmed module layout and import targets.

- [x] **Step 3: Fix `tool_chain_env_gym.py` bare imports**
   - Difficulty: 6/10
   - Est. Time: 20 min
   - Why now: App startup is currently blocked at import/runtime initialization layer.
   - Output expected: `uvicorn tool_chain_env.app:app` starts and `/health` returns 200.

- [x] **Step 4: Now fix all the bugs in the server files**
   - Difficulty: 7/10
   - Est. Time: 25 min
   - Why now: If episode data never reaches mock store, tasks fail regardless of API correctness.
   - Output expected: Auth + task endpoints behave correctly after reset.

- [x] **Step 5: Now fix all the bugs in the test files**
   - Difficulty: 5/10
   - Est. Time: 15 min
   - Why now: Signature/return mismatch can cause terminal grading errors.
   - Output expected: Grader works for in-progress and terminal states with valid score outputs.

- [x] **Step 6: Fix `Dockerfile` `CMD` instruction**
   - Difficulty: 6/10
   - Est. Time: 25 min
   - Why now: Current tests include endpoint assumptions that no longer match implementation.
   - Output expected: Task happy-path and core behavioral tests align to actual routes.

- [x] **Step 7: Fix `requirements.txt` missing `pydantic-settings`**
   - Difficulty: 4/10
   - Est. Time: 10 min
   - Why now: Prevent secondary regressions during benchmark/training checks.
   - Output expected: Wrapper imports resolve under package execution.

- [x] **Step 8: Fix `README.md` baseline command**
   - Difficulty: 7/10
   - Est. Time: 35 min
   - Why now: Confirms real stability after edits, not just syntax-level fixes.
   - Output expected: passing import check, app startup check, targeted pytest subset, baseline run.

- [x] **Step 9: Fix `baseline/run_baseline.py` task5 bug**
   - Difficulty: 3/10
   - Est. Time: 10 min
   - Why now: Prevent false-complete tracking and keep final push controlled.
   - Output expected: Implemented-vs-verified state is accurate.

## Work We Are Intentionally Skipping For Now (External/Blocked)

1. HuggingFace Spaces deployment proof
   - Reason: External deployment environment and secrets/config may be required.

2. Final OpenEnv remote validation proof capture
   - Reason: Depends on deployment endpoint and external validation context.

3. Final Docker publish-level hardening
   - Reason: Useful, but not needed for immediate 98% internal stabilization.

## 98% Done Criteria (This Phase)

1. App starts locally without crash.
2. Health endpoint responds correctly.
3. Core task flows execute after reset without empty-store failures.
4. Grader returns valid scores and no runtime mismatch errors.
5. Key tests pass (especially startup, reset isolation, task correctness).
6. Baseline run completes with valid SCORE lines.
7. Checklist reflects verified status accurately.

## Estimated Total For This 98% Phase

- Estimated time: **155 minutes** (about 2h 35m)
- Confidence if executed in order: **High**
